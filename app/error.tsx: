"use client";

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <main className="min-h-screen bg-zinc-50 dark:bg-zinc-950 flex items-center justify-center">
      <div className="text-center">
        <h2 className="text-3xl font-bold mb-4">Something went wrong</h2>
        <p className="text-zinc-600 dark:text-zinc-400 mb-8">{error.message}</p>
        <button
          onClick={reset}
          className="px-8 py-3 bg-zinc-900 text-white rounded-2xl hover:bg-black"
        >
          Try again
        </button>
      </div>
    </main>
  );
}
